# Phase 1: reproducible, tracked training baseline.
# Every module imported by this project is listed here -- no implicit deps.

# Feature contract shared with the phase-2 inference service. Editable install
# so training and serving always compile against the same source.
# Relative to this directory: run `pip install -r requirements.txt` from phase1-mlops/.
-e ../shared

mlflow>=3.1,<4.0
scikit-learn>=1.3,<2.0
pandas>=2.0,<3.0

# Test runner for tests/test_train.py.
pytest>=8.0,<10.0
